"""ClarQ integration for the LLM-Proactive prompting strategy.

The implementation imports ``clarq_eval`` from the Huawei checkout at runtime,
so use ``run_evaluation.py`` or add that evaluator directory to ``sys.path``
before importing the policy modules directly.
"""
